export const FLEET_MESH_AVAILABLE_FLAG = 'FLEET_MESH_AVAILABLE';

// MultiClusterHub is registered only on ACM hub clusters, never on managed spokes.
export const ACM_HUB_MULTICLUSTERHUBS_URL =
  '/api/kubernetes/apis/operator.open-cluster-management.io/v1/multiclusterhubs';
